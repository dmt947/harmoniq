import 'package:flutter/material.dart';

class AIFeatures extends StatelessWidget {

  const AIFeatures({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Pendiente...'));
  }
}
